<span class="realtime-status @(Connected ? "connected" : "disconnected")" role="status"><span class="status-dot"></span>@(Connected ? "Live" : "Reconnecting…")</span>
@code { [Parameter] public bool Connected { get; set; } }
