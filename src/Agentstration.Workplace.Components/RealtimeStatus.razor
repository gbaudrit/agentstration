<span class="realtime-status @(Connected ? "connected" : "disconnected")" role="status"><span class="status-dot"></span>@(Connected ? Localizer["Live"] : Localizer["Reconnecting"])</span>
@code { [Parameter] public bool Connected { get; set; } }
