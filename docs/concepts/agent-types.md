# Direct agent definitions

Agents are self-contained declarative resources. Reusable behavior is composed through named tools, behaviors, middleware, and context providers instead of an inheritance layer. See [Agents](agents.md).
