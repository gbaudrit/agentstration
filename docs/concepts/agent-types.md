# Agent types

An Agent Type is a reusable, versioned behavioral template referenced by Agents. It is not an Agent instance: many Agents can use the same type while supplying their own name, model profile, instructions, or tools.

Agent Types are Management resources and can be published in immutable versions. See the existing [Agent Types note](../agent-types.md) for the current inheritance rules.
