# Resources

Management resources use a declarative, Azure-inspired envelope with a canonical resource type, dated `apiVersion`, name, resource group, location, tags, desired `properties`, and observed `status` where applicable.

Resources are not runtime instances. They are governed declarations from which runtime state can be reconstructed. See the [resource reference](../reference/resources/overview.md).
