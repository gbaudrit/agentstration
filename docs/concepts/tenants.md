# Tenants

**Not implemented yet.** Tenant is reserved for a future isolation and governance boundary broader than a Workspace. The current standalone product has no Tenant resource, tenant-aware route, or tenant persistence boundary.

Do not use “tenant” as a synonym for [Workspace](workspaces.md). Any future Tenant model must define its relationship to Workspace and preserve Workspace-scoped access rules.
