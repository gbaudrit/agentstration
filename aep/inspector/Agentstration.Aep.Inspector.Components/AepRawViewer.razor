<div class="section-heading"><div><p class="eyebrow">Canonical discovery</p><h2>Raw manifest</h2><p>Formatted response from <code>/.well-known/aep</code>.</p></div><span class="protocol-badge">JSON</span></div>
<pre class="raw-viewer">@Json</pre>
@code { [Parameter] public string Json { get; set; } = ""; }
