# Contributing

Open an issue before a large change. Use focused branches, add MSTest coverage, and run `dotnet test Agentstration.slnx` before submitting a pull request. Never commit secrets or document contents used for testing.
